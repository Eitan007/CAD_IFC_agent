# Core API
fastapi==0.111.0
uvicorn[standard]==0.29.0
python-multipart==0.0.9
pydantic==2.7.1
pydantic-settings==2.2.1

# IFC / CAD parsing
ifcopenshell==0.7.10  # PyPI wheel (0.7.0.240406 removed); conda still optional — see README
# FreeCAD is system-installed; OCC (OpenCascade) via:
# conda install -c conda-forge pythonocc-core

# Database (PostgreSQL + async)
sqlalchemy==2.0.30
asyncpg==0.29.0
alembic==1.13.1
psycopg2-binary==2.9.9

# Neo4j (optional — swap storage backend)
neo4j==5.20.0

# LLM
anthropic>=0.40.0
openai==1.30.1           # fallback

# RAG / vector store
chromadb==0.5.0
sentence-transformers==2.7.0

# Utilities
httpx==0.27.0
aiofiles==23.2.1
python-dotenv==1.0.1
structlog==24.1.0
tenacity==8.3.0

# Dev / test
pytest==8.2.1
pytest-asyncio==0.23.7
httpx==0.27.0
ruff==0.4.5
